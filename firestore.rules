rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo usuarios autenticados (incluyendo anónimos) pueden leer/escribir su propio data
    match /neomonitor/{collection}/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
